<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @question.content %>
</p>
<%= image_tag @question.question_image.url if @question.question_image? %>
<%= link_to 'File Attchment', @question.file_attachment.url if @question.file_attachment? %>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>
